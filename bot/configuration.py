Python 3.12.6 (tags/v3.12.6:a4a2d2b, Sep  6 2024, 20:11:23) [MSC v.1940 64 bit (AMD64)] on win32
Type "help", "copyright", "credits" or "license()" for more information.
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)

while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
    if current_spread > spread:
        update_order_book()

    # Wait for the next update
    time.sleep(1)
while True:
    # Get the current price on DEX
    dex_price = dex.fetch_ticker(symbol)['last']

    # Get the current price on CEX
    cex_price = cex.fetch_ticker(symbol)['last']

    # Calculate spread
    current_spread = abs(dex_price - cex_price)

    # If the spread is greater than desired, update the order book
...     if current_spread > spread:
...         update_order_book()
... 
...     # Wait for the next update
...     time.sleep(1)
... 
... while True:
...     # Get the current price on DEX
...     dex_price = dex.fetch_ticker(symbol)['last']
... 
...     # Get the current price on CEX
...     cex_price = cex.fetch_ticker(symbol)['last']
... 
...     # Calculate spread
...     current_spread = abs(dex_price - cex_price)
... 
...     # If the spread is greater than desired, update the order book
...     if current_spread > spread:
...         update_order_book()
... 
...     # Wait for the next update
...     time.sleep(1)
... 
... while True:
...     # Get the current price on DEX
...     dex_price = dex.fetch_ticker(symbol)['last']
... 
...     # Get the current price on CEX
...     cex_price = cex.fetch_ticker(symbol)['last']
... 
...     # Calculate spread
...     current_spread = abs(dex_price - cex_price)
... 
...     # If the spread is greater than desired, update the order book
...     if current_spread > spread:
...         update_order_book()
... 
...     # Wait for the next update
...     time.sleep(1)
